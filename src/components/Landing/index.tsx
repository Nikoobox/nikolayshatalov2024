import { FC } from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const Landing: FC = () => {
  return (
    <Box mt={10}>
      <Typography>Hello from Landing</Typography>
    </Box>
  );
};
export default Landing;
